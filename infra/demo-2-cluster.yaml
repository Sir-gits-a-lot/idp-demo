---
apiVersion: osm.io/v1alpha1
kind: ClusterClaim
metadata:
  name: demo-2
  namespace: osm
  labels:
    cluster-owner: tataelxsi
spec:
  id: demo-2
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: eks
  parameters:
    nodeSize: small
    version: "1.29"
    minNodeCount: 1
