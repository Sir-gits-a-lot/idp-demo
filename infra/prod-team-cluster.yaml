---
apiVersion: osm.io/v1alpha1
kind: ClusterClaim
metadata:
  name: prod-team
  namespace: osm
  labels:
    cluster-owner: tataelxsi
spec:
  id: prod-team
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: eks
  parameters:
    nodeSize: small
    version: "1.29"
    minNodeCount: 1
