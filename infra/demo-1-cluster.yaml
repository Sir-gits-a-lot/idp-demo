---
apiVersion: osm.io/v1alpha1
kind: ClusterClaim
metadata:
  name: demo-1
  namespace: osm
  labels:
    cluster-owner: tataelxsi
spec:
  id: demo-1
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: ck-all
  parameters:
    nodeSize: small
    version: "1.29"
    minNodeCount: 1
