---
apiVersion: osm.io/v1alpha1
kind: ClusterClaim
metadata:
  name: devconf-eks
  namespace: osm
  labels:
    cluster-owner: tataelxsi
spec:
  id: devconf-eks
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: eks
  parameters:
    region: IN
    nodeSize: small
    version: "1.31"
    minNodeCount: 1
