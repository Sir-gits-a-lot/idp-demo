---
apiVersion: xcloud.io/v1alpha1
kind: ClusterClaim
metadata:
  name: devconf
  labels:
    cluster-owner: utkarsh
spec:
  id: devconf
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: eks
  parameters:
    region: IN
    nodeSize: small
    version: "1.32"
    minNodeCount: 1
